import DefaultPanelLayout from "@/layouts/panels/default-panel-layout";

// use Generic CRUD Manager

export default function Workspace() {
  return <DefaultPanelLayout breadcrumbs={[]} children={<div>Workspace</div>} />;
}